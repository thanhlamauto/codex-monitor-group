# Web UI

The dashboard is intentionally server-rendered from `server/app/templates` and `server/app/static`. This placeholder preserves the monorepo boundary for a future standalone frontend without adding a Node container to the MVP.
